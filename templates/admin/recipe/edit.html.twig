{% extends 'admin/admin.html.twig' %}
{% block title %}
	Editer recette :
	{{recipe.title}}
{% endblock %}

{% block body %}
	<h1>Editer la recette</h1>
	{{ form_start(form) }}
	<div class="d-flex">
		{{ form_row(form.title) }}
		{{ form_row(form.duration) }}
	</div>
	{{ form_rest(form) }}
	{{ form_end(form) }}
{% endblock %}
